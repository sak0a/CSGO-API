services:
  csgo-api:
    build: .
    environment:
      - UPDATE_INTERVAL=3600
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost/api/en/all.json"]
      interval: 30s
      timeout: 5s
      retries: 3
